<div class="container-devise-user-mobile">
  <div class="form-inputs">
    <div class="form-title ">
      <h2>Welcome, <%= @resource.name %>!</h2>
    </div>

    <p>
    Someone has requested a link to change your password.<br>
    You can do this through the link below.</p>

    <div class="form-actions">
      <%= link_to 'Change my password', edit_password_url(@resource, reset_password_token: @token), :class => "btn btn-primary btn-submit w-100%" %>
    </div>

    <p>If you didn't request this, please ignore this email.</p>
    <p>Your password won't change until you access the link above and create a new one.</p>

  </div>
</div>
